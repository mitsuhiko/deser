[package]
name = "crate-path"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# deser under a different name, so `::deser` does not exist in this crate
serialization = { package = "deser", path = "../../deser", features = ["derive"] }
deser-json = { path = "../../deser-json" }
